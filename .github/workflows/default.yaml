name: 'Hotel Reserva Build'

'on':
  push: { branches: [main] }

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Build
        run: npm install && npm run build
